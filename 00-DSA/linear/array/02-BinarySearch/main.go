package main

import "fmt"

func binarySearch(arr []int, target int) int {
	start := 0
	end := len(arr) - 1
	for start < end {
		mid := (start + end) / 2
		if arr[mid] == target {

		}
	}
	return -1
}
func main() {
	fmt.Println("Hello")
}
